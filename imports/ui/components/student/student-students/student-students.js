import React from 'react';
import { DashboardContainer } from '../../common'
import StudentsList from '../../common/StudentsList/StudentsListContainer'






export const StudentStudents = () => (
  	<DashboardContainer pageTitle={'Students'}>
  		<StudentsList />
  	</DashboardContainer>
);